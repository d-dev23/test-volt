# Взял инфу с оф сайта гитлаб первые 5 пунктов я полагаю сделаны  

job_using_vault:
  id_tokens:
    VAULT_ID_TOKEN:
      aud: https://volt-test-01.ru
  secrets:
    DATABASE_PASSWORD:
      vault: #путь до 1 сикрета 
      token: $VAULT_ID_TOKEN1
  secrets2:
    DATABASE_PASSWORD:
      vault: #путь до второго сикрета   
      token: $VAULT_ID_TOKEN2
  secrets3:
    DATABASE_PASSWORD:
      vault: #путь до третьего сикрета   
      token: $VAULT_ID_TOKEN3
